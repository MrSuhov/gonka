################################################################################
# Gonka Node Configuration Example
#
# Скопируйте этот файл и переименуйте, например: node1.conf
# Заполните все обязательные параметры перед запуском
################################################################################

# ============================================================================
# ОБЯЗАТЕЛЬНЫЕ ПАРАМЕТРЫ
# ============================================================================

# SSH и сетевые параметры
SERVER_IP="185.216.20.162"                    # IP адрес вашего сервера
SSH_USER="ubuntu"                              # SSH пользователь (обычно ubuntu)

# Параметры ноды
KEY_NAME="quick-newton"                        # Имя ноды (как сгенерировал хостер)
KEYRING_PASSWORD="kjhjfjfjfbe5rig7hkjnf"       # Рандомный пароль для warm key (минимум 8 символов)
ACCOUNT_PUBKEY="A6Mh+ZZil8mCUSIJG9iWLj/IH5PfCJaMoAX3U70c2GE/"  # Public key из local_key.txt

# Модель для скачивания
# Для 1 GPU используйте: Qwen/Qwen3-32B-FP8
# Для 8 GPU используйте: Qwen/Qwen3-235B-A22B-Instruct-2507-FP8
MODEL_NAME="Qwen/Qwen3-32B-FP8"

# Seed node (обычно один из стандартных)
SEED_API_URL="http://node2.gonka.ai:8000"

# Профиль конфигурации ноды
# x1 - для одной GPU (H100/A100)
# x8 - для 8 GPU
NODE_CONFIG_PROFILE="x1"

# ============================================================================
# ОПЦИОНАЛЬНЫЕ ПАРАМЕТРЫ (можно не указывать, будут использованы дефолты)
# ============================================================================

# Пути на сервере
#HF_HOME="/mnt/ssd/hf"                         # Где хранить модели
#GONKA_PATH="/mnt/ssd/gonka"                   # Где клонировать Gonka репозиторий
#VOLUME_DEVICE="/dev/vdc"                      # Volume устройство (обычно всегда vdc)
#VOLUME_MOUNT="/mnt/ssd"                       # Точка монтирования volume

# Persistent peers (если не указано, будут использованы дефолтные)
# Раскомментируйте если хотите переопределить дефолтные peers:
#PERSISTENT_PEERS="981908092bc597e60cc81eda4329783aea7af9d7@85.234.66.95:5000,0aaa255c5b119e95cd66e1bd6032b213ce1c7943@85.234.66.223:5000,8e99e6adee695719c1c0ed5a37165e14f4c0751f@85.234.66.191:5000"

################################################################################
# ПРИМЕЧАНИЯ
################################################################################
#
# 1. ACCOUNT_PUBKEY получается при создании холодного кошелька локально:
#    ./inferenced keys add node-name --keyring-backend file
#    Сохраните вывод в local_key.txt
#
# 2. KEY_NAME должен совпадать с именем директории для этой ноды
#
# 3. KEYRING_PASSWORD - это пароль для ТЕПЛОГО кошелька на сервере,
#    НЕ путайте с паролем для холодного кошелька!
#
# 4. После успешного деплоя НЕ ЗАБУДЬТЕ выполнить локально команду grant:
#    ./inferenced tx inference grant-ml-ops-permissions \
#        <cold-key-name> \
#        <warm-wallet-address> \
#        --from <cold-key-name> \
#        --keyring-backend file \
#        --gas 2000000 \
#        --node http://node2.gonka.ai:8000/chain-rpc/
#
#    Где:
#    - cold-key-name: имя ключа из local_key.txt
#    - warm-wallet-address: адрес из nodes/<KEY_NAME>/warm_address.txt
#
################################################################################
